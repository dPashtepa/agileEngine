import React from 'react';
import AddedProducts from '../containers/added_products';
import Form from '../containers/form';

const Home = () => (
  <div className="home">
    <Form />
    <AddedProducts />
  </div>
);

export default Home;
